# Repository-as-State handoff — 2026-09-03 23:19 Europe/London

## Resume instruction

Read:

1. `aidocs/repository-as-state/CURRENT.md`
2. this handoff
3. `aidocs/repository-as-state/PROGRAMME-MATRIX.md`
4. `aidocs/repository-as-state/SESSION-2026-09-03.md`

Programme: **59/67 complete**.

Active item: **60 — implement and qualify genuine P2 semantic hidden verifiers**.

## Frozen P2 inputs

Design-input manifest:

`D65E88E8A49114554DBF9C911144BF998172FDD494757AEB6F4491EB9C1F88AF`

Task-selection freeze:

`829CD09A84616C5282AFAED1FB4EDA168BE238E344E02EC8AE54A8C11D6ADE94`

Curation package:

`5832646A66A37E7F47F124796EEE4E289F72584424E37E2E600970FDC929F6A6`

Tasks/behaviours:

- P2_T01 C06: PRE `a120b02abf4dd2bef11ae621d7283282159622f3`; POST `ddb2d79c65342e8585ef5aefe966e7b2e70b9406`; MEDIUM; 4 behaviours.
- P2_T02 C07: PRE `5941869627443548a9042d900b1a4ffeda58dacb`; POST `c265580ac53a4e85a164a7f62a5b90f3ecf04cfe`; MEDIUM; 3 behaviours.
- P2_T03 C08: PRE `142a007c64d1c20136742155b672055100128056`; POST `a740ce1965ba26ab5e06ed5c466430f1e28c5ac5`; LOW; 3 behaviours.
- P2_T04 C09: PRE `c3b813d14973c28ed3bc063c2440224b26dc2a87`; POST `d7199f391983eb94bb48d8524915245898831a3a`; LOW; 4 behaviours.
- P2_T05 C10: PRE `e9eb063944d604ec0c4cf6b3534f5db87fff82c0`; POST `a64a727d1ce22dfb851419e46958f08014a48b04`; LOW; 4 behaviours.

Total behaviours: 18.

## Rejected provisional verifier attempt

The previous worker materialised real PRE/POST workspaces but then implemented `oracle_runner.py` whose behavioural result was read from hand-authored semantic-state JSON.

That is not a valid candidate verifier.

Therefore all apparent results generated by that runner are superseded/non-evidence:

- PRE/POST discrimination;
- 18/18 negative controls;
- alternate-valid passes;
- self-test qualification;
- provisional task verifier package hashes;
- provisional overall verifier freeze.

Do not use any of those in preregistration.

The previous worker eventually identified this problem itself and correctly stated that those provisional artifacts must not enter P2 preregistration.

Its `VALID_TERMINAL_B` classification is rejected by the coordinator because normal verifier engineering remains. No frozen scientific contract needs changing.

## What the next worker must do

Reimplement each verifier as a genuine semantic probe of the actual candidate workspace.

Permitted patterns include:

- compile a private probe against the candidate's public/stable assembly/API;
- load candidate assemblies dynamically and invoke stable public/domain contracts;
- launch candidate process/app and exercise public HTTP/process behaviour;
- use reflection over stable public interfaces/types where version variation requires it;
- inject deterministic local fakes/fixtures through existing public/stable seams;
- build a stable adapter layer that maps historical versions to the same semantic observation.

The verifier must not derive correctness from source literals, historical tests, helper names or expected-result JSON.

Fixture data may provide deterministic inputs or dependency responses but may not state whether the candidate passes.

## Genuine controls

Negative controls:

- start from an independently materialised POST candidate copy;
- change the candidate implementation so exactly the target semantic behaviour is violated while the candidate remains runnable;
- run the same semantic verifier against that candidate;
- require the target defect to be detected;
- total genuine controls: 18.

Alternate-valid controls:

- create real candidate implementation variants differing materially in internal implementation/decomposition/naming/control flow;
- preserve the disclosed semantics;
- require the same verifier to pass them.

## Required final gate

Before item 60 can become complete:

- all five historical PRE controls HARNESS_VALID + overall FAIL;
- all five historical POST controls HARNESS_VALID + all behaviours PASS;
- 18 genuine semantic negative controls detected;
- all five alternate-valid qualification sets pass;
- implementation independence PASS for every task;
- verifier self-tests PASS;
- PRE/POST determinism x3 PASS;
- hidden verifier inaccessible to experimental users while coordinator access remains;
- five real final task-verifier hashes;
- one real final overall verifier package hash;
- no P2 task-solving model invocation;
- P2 experimental runs remain 0;
- P0/P1 not rerun;
- frozen design/task/curation hashes unchanged.

## Terminal rule

Do not return `VALID_TERMINAL_B` while a genuine candidate-workspace semantic verifier can still be engineered without changing frozen requirements/identities.

Ordinary build/restore/reflection/API/adapter/fixture/ACL/test-harness problems must be solved and rerun.

## Next stage after successful item 60

Coordinator will:

1. accept and publish public-safe verifier qualification;
2. update aidocs/matrix to 60/67;
3. freeze P2 final preregistration, exact prompts, randomisation, session topology, runtime/model, output-freeze mechanism, blind-adjudication gate and execution lock;
4. only then permit the 30 P2 model runs.