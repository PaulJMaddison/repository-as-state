# Repository-as-State handoff — 2026-09-03 23:27 Europe/London

## Resume order

1. `aidocs/repository-as-state/CURRENT.md`
2. this handoff
3. `aidocs/repository-as-state/PROGRAMME-MATRIX.md`
4. `aidocs/repository-as-state/SESSION-2026-09-03.md`
5. `results/public/subject-b-p2-corpus-freeze-correction-v1.md`

## Current programme position

**58/67 complete.**

**Item 59 reopened and active.**

Item 60 is paused.

P2 experimental runs: **0**.

## Why item 59 reopened

The genuine candidate-workspace verifier remediation attempted to build the exact previously frozen P2_T03/C08 POST:

`a740ce1965ba26ab5e06ed5c466430f1e28c5ac5`

and found a compile-time `CS0535` contract failure.

Both:

- `InMemoryMarketInventoryRepository`
- `PostgresMarketInventoryRepository`

fail to implement the newly declared:

`IMarketInventoryRepository.ReconcileCompleteSourceSnapshotAsync(...)`

Therefore this commit cannot currently be treated as a validated accepted POST boundary.

The worker's `VALID_TERMINAL_B` is accepted for this run because continuing verifier qualification would require changing a frozen task identity/candidate state.

## Superseded initial P2 freezes

- design input: `D65E88E8A49114554DBF9C911144BF998172FDD494757AEB6F4491EB9C1F88AF`
- initial task-selection freeze: `829CD09A84616C5282AFAED1FB4EDA168BE238E344E02EC8AE54A8C11D6ADE94`
- initial curation package: `5832646A66A37E7F47F124796EEE4E289F72584424E37E2E600970FDC929F6A6`

These identify the historical initial freeze but are not final P2 preregistration identities.

## Active next work package

**P2 CORPUS BOUNDARY METHODOLOGY REPAIR — C08 / FULL SELECTED-POOL REVALIDATION**

The next worker must:

1. prove the T03 compile defect independently from exact historical source state;
2. inspect C08 history and contemporaneous requirement/acceptance evidence;
3. determine whether `a740ce1...` was an intermediate/non-accepted commit;
4. if a later legitimate accepted POST for C08 exists, prove it from historical evidence and use that boundary;
5. otherwise reclassify C08 ineligible under the already-existing accepted-boundary/deterministic-execution criteria;
6. mechanically recompute all 10 candidates under the same criteria;
7. do not substitute excluded tasks to preserve a five-task count;
8. mechanically materialise, restore and build/test every PRE/POST that remains selected;
9. verify every selected POST is executable and can support behavioural contract qualification;
10. recompute task count, complexity distribution, planned run count and topology if eligibility changes;
11. re-curate only the artifacts affected by corrected task identities/counts;
12. create superseding v2 task-selection/curation freezes with explicit lineage to the invalidated v1 freeze;
13. generate public-safe correction/freeze drafts;
14. keep P2 experimental model runs at zero.

## Forbidden

- do not patch C08 POST to make it compile and still call it historical POST;
- do not choose a later commit merely because it passes;
- do not invent new selection criteria from model/verifier outcomes;
- do not substitute another excluded candidate merely to keep N=5;
- do not start P2 verifier qualification until corrected corpus freeze is accepted;
- do not run P2;
- do not rerun P0 or P1.

## Coordinator action after next Codex return

In the same response:

1. assess whether the historical-boundary repair is valid;
2. update `CURRENT.md`, matrix, session and new handoff;
3. publish only public-safe superseding freeze/correction evidence;
4. if accepted, mark item 59 complete again and item 60 active;
5. issue a new genuine semantic-verifier qualification prompt against the corrected corpus.
