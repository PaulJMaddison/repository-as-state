# Repository-as-State handoff — 2026-09-04 15:39 Europe/London

## Authoritative state

- programme: **60/67 complete**
- Item 61: **ACTIVE — restricted Codex runtime-access repair and superseding execution relock**
- Item 62: **BLOCKED until Item-61 repair/relock is accepted**
- Items 63–67 pending
- fresh scheduled Item-62 units with genuine model activity: **0/24**
- fresh accepted P2 units: **0**
- hidden verifier runs against fresh candidates: **0**
- correctness adjudications: **0**
- P2 fresh experiment executed: **FALSE**
- P0 rerun: **FALSE**
- P1 rerun: **FALSE**

## Triggering falsification

The first live Item-62 preflight proved:

- restricted credential valid;
- identity `DESKTOP-BFTREBH\ras-p2-experimental` non-admin;
- `Start-Process -Credential` for the frozen Codex runtime returns `Access is denied` before any model process starts;
- no experimental attempt was consumed.

Worker terminal:

`BLOCKED_PRE_MODEL_RESTRICTED_CODEX_EXECUTABLE_ACCESS_DENIED`

Public falsification evidence:

- `results/public/subject-b-p2-item61-runtime-access-falsification-v2.json`
- `results/public/subject-b-p2-item61-runtime-access-falsification-v2.md`

The previous Item-61 execution lock remains historical evidence but is superseded for live execution readiness.

## Frozen experimental choices that MUST NOT change

- target model: `gpt-5.6-luna`
- Codex CLI target version: `codex-cli 0.153.0-alpha.5`
- task selection: C07/C08/C09/C10
- prompt manifest: `DE05F68970B7E4DDE86990335C3621C88C36FB880C86591FC207131ACF164CC4`
- randomisation manifest: `6878648FBB56E03F2BA7EECA6AD17FAB8760203F115CAA515CD3736CAB7D3947`
- experimental-unit manifest: `73DD340957D5850FE059E07F25DAFB84101E9E72C03B3245C5009D07DB6F82BF`
- blind-ID mapping: `CB6AB22AA185E45FF085C89104086743DA26262274BE053ECF40768646A9D498`
- PRE-workspace manifest: `209FA8ADA0282569B43A4A626DE3750853671CAC268CA5D5A19DC388ED980456`
- timeout: 1800 seconds
- repetitions are not retries
- no best-of-N
- no human rescue
- full-block blindness
- all 24 outputs before hidden adjudication

The randomisation seed, schedule, blind mapping and hidden verifier remain sealed.

## Repair objective

Make the exact frozen Codex runtime executable under the restricted experimental identity **without weakening the protected coordinator/private filesystem boundary**.

Preferred architecture:

`C:\Kyntic\ras-p2-experimental-runtime-v2`

This root may be readable/executable by `DESKTOP-BFTREBH\ras-p2-experimental` and coordinator/Admin, but must contain only runtime/tooling bytes needed for task execution — no methodology, hidden verifier, sealed schedule/blind mapping, coordinator profile, credentials or experiment output.

Do NOT solve the problem by granting the experimental identity access to:

- `C:\Kyntic\ras-p2-private`
- `C:\Kyntic\repository-as-state`
- `C:\Users\pm\.codex`
- coordinator user-profile roots
- hidden verifier/methodology roots
- contaminated execution evidence

## Required diagnosis

Determine the real runtime chain before changing anything:

- `Get-Command codex -All`
- `where.exe codex`
- inspect whether the command is an exe, cmd/ps1 shim or package entry point;
- resolve the final executable and every required adjacent runtime/package dependency;
- inspect ACLs on each path in that chain;
- verify which exact path returns Access Denied for the restricted identity.

If Codex is installed through npm/shims, do not merely copy the shim. Copy/provision the exact package/runtime dependency chain required to reproduce `codex-cli 0.153.0-alpha.5` under the experimental runtime root.

If it is self-contained, copy the exact required executable/adjacent files.

Preserve source→experimental byte identities with SHA-256 where the files are supposed to be byte-identical.

## Required repair

Create or repair the isolated experimental runtime root.

Grant the restricted identity only the minimum read/execute permissions needed on that runtime root.

Do not grant write access unless Codex requires a narrowly-scoped mutable subdirectory; if mutable state is required, separate executable/runtime bytes from mutable experimental Codex state and document the boundary.

Use the already-separated experimental Codex home:

`C:\Kyntic\ras-p2-experimental-home-v2`

Do not point the experimental process at `C:\Users\pm\.codex`.

If authentication/runtime state is missing from the experimental home, solve that through the supported restricted-user Codex authentication path or existing already-qualified experimental state. Do not expose or copy coordinator-private methodology. Never write plaintext credentials to artifacts.

## Mandatory live zero-task qualification

After repair, under `DESKTOP-BFTREBH\ras-p2-experimental`, prove all of the following **before any scheduled Item-62 task execution**:

1. `whoami` returns the exact restricted identity;
2. identity is non-admin/non-elevated;
3. experimental runtime executable starts successfully;
4. `codex --version` returns exactly `codex-cli 0.153.0-alpha.5`;
5. exact model `gpt-5.6-luna` is available using a zero-completion/non-task check;
6. experimental workspace read/write probe passes;
7. protected private/coordinator roots remain denied;
8. absolute-path private file reads remain denied;
9. protected-directory enumeration remains denied;
10. no symlink/reparse/broker bypass is introduced;
11. child task process is not more privileged than experimental parent;
12. plaintext credentials in artifacts/logs/templates = 0.

A harmless `whoami`, `codex --version` or zero-completion model availability check does not consume an experimental task unit.

## Worker behaviour

This is a repair worker. **Do not stop at the first ordinary ACL/path/shim/runtime error. Diagnose and fix it within the boundary.**

Try the safe isolated-runtime repair path fully.

Do not ask the user routine implementation questions.

Do not repeatedly reopen credential dialogs. If a credential must be obtained, use one persistent in-memory credential host that survives controller errors for the duration of the qualification. Never persist the password.

If a tooling attempt fails before model activity, repair it and continue. Preserve evidence of failures without treating them as consumed experimental units.

Only stop if you have exhausted safe isolated-runtime approaches or fixing the issue would require weakening protected-root isolation / changing frozen experimental choices.

## Fresh superseding lock

Once live qualification passes, create a new private root:

`C:\Kyntic\ras-p2-private\subject-b-p2-v2\execution-lock-v3`

Preserve the old v2 lock untouched as historical evidence.

Create deterministic evidence for:

- runtime-path root-cause audit;
- source runtime identity/hash inventory;
- experimental runtime identity/hash inventory;
- ACL repair manifest;
- restricted process-token audit;
- protected-root negative-access requalification;
- experimental workspace positive-access requalification;
- exact Codex CLI/model runtime requalification;
- prompt/schedule/blind/PRE immutability audit;
- zero-task-model gate;
- superseding execution-lock manifest;
- contents manifest;
- deterministic execution-lock package.

Create public-safe drafts under:

`C:\Kyntic\ras-p2-private\subject-b-p2-v2\public-ready-v2`

with names:

- `subject-b-p2-execution-lock-v3.json`
- `subject-b-p2-execution-lock-v3.md`

Do not expose seed, schedule, blind mapping, hidden verifier, credentials or correctness.

Do not modify the public GitHub repository.

## Success state

A successful repair worker leaves:

- scheduled Item-62 model-active units: 0
- Item-62 correctness adjudications: 0
- hidden verifier runs against fresh candidates: 0
- old v2 lock preserved
- experimental choices unchanged
- isolated runtime live-qualified under restricted identity
- protected-root isolation requalified
- new v3 execution lock created
- public-safe v3 lock drafts created
- Item 61 not marked complete by worker
- Item 62 not started by worker

Success terminal:

`WORKER_TERMINAL_STATE=VALID_TERMINAL_A_ITEM61_RUNTIME_ACCESS_REPAIRED_V3_LOCK_READY_FOR_COORDINATOR_ACCEPTANCE`

Coordinator then validates/publishes v3, restores Item 61 to COMPLETE and immediately reactivates Item 62.
