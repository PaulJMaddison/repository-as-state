# Repository-as-State — Session 2026-09-05 23:10 Europe/London

## Programme state

- Authoritative progress ledger remains **64/67 complete**.
- Item 65 remains **ACTIVE**.
- Item 66 remains blocked.
- P0 rerun: **FALSE**.
- P1 rerun: **FALSE**.
- P2 task rerun: **FALSE**.

## Subject C execution event

The locked Subject-C 18-run execution began from the accepted detached execution boundary:

`84e68b18c8c58d5576d6f726a51524d3e676758f`

Accepted execution-lock package:

`9538cc1b9af91b732127d08085a7f2211f37f15fd5bd091d8b03a2493f770afe`

During the first scheduled unit, the restricted runtime reached genuine model activity but failed with HTTP 401 authentication before producing a completion.

Current execution facts:

- scheduled units: **18**
- model-active units: **1**
- completed model completions: **0**
- slot 1: **CONSUMED_FAILED_AFTER_MODEL_ACTIVITY**
- slot 1 failure: **401_UNAUTHORIZED_MISSING_AUTHENTICATION**
- slot 1 rerun permitted: **FALSE**
- remaining units started: **0/17**
- hidden-verifier runs against model output: **0**
- correctness adjudications: **0**
- project build/compiler/test/lint runs: **0**
- public execution checkout remained clean at the frozen execution boundary

The consumed slot must be preserved permanently. It must not be rerun after authentication repair.

## Recovery rule

This is treated as an infrastructure/runtime-authentication defect discovered after one unit crossed the model-activity boundary.

The established recovery principle remains:

- preserve every model-active unit exactly as observed;
- never rerun a consumed unit;
- infrastructure may be repaired for later, not-yet-started units if the repair does not alter the frozen model, task contracts, schedule, semantic verifier, condition mapping, repetition structure, workspace bytes or causal treatment;
- no quality retry, best-of-N or human rescue;
- no build/test/compiler/lint substitution;
- no correctness inspection during execution.

Slot 1's post-attempt workspace state must be frozen as the candidate state for that scheduled unit, including its unchanged source/state snapshot if no source modification occurred, together with the authentication failure telemetry.

After authentication is repaired under the restricted identity, execution may resume only from the next unstarted scheduled unit. Slot 1 remains consumed.

If slot 1 belongs to Condition A, the exact session identity created for that chain must be preserved. If the next A task cannot resume that exact session without changing the frozen treatment, execution must stop rather than substitute a fresh session.

## Authentication repair constraints

Authentication repair must be zero-task-model activity.

Permitted:

- authenticate the already-qualified restricted identity for the already-bound Codex runtime;
- use the runtime's login/authentication flow under `DESKTOP-BFTREBH\ras-p2-experimental`;
- read `codex login status` after authentication;
- repair launcher transport only where necessary to make the already-frozen invocation work.

Prohibited:

- copy coordinator credentials/auth files into the restricted profile;
- change model or CLI version;
- change schedule, prompts, task contracts, verifier or workspaces;
- rerun slot 1;
- run any unscheduled task-solving completion as an auth probe;
- run builds, compilers, tests or lint;
- inspect correctness.

## Current next action

Perform a zero-task-model authentication repair under the restricted identity, prove the bound runtime is authenticated without generating a completion, freeze slot 1 as the consumed failed candidate state, and then continue the existing schedule from the next unstarted slot if and only if all frozen treatment constraints remain satisfiable.
