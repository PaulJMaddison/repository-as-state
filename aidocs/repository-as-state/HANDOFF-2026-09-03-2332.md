# Repository-as-State handoff — 2026-09-03 23:32 Europe/London

## Resume order

1. `aidocs/repository-as-state/CURRENT.md`
2. this handoff
3. `aidocs/repository-as-state/PROGRAMME-MATRIX.md`
4. `aidocs/repository-as-state/SESSION-2026-09-03.md`
5. `results/public/subject-b-p2-corpus-freeze-correction-v2.md`

## Current programme position

**59/67 complete.**

**Item 60 active:** implement and qualify genuine P2 semantic hidden verifiers.

P2 experimental runs: **0**.

## Coordinator correction

A prior verifier-remediation prompt incorrectly required full `SearchForCars.sln` build/test success and treated a full-solution P2_T03 compile failure as evidence that the frozen accepted boundary was invalid.

That requirement was not part of the established methodology.

The temporary corpus-reopen/correction-v1 state is superseded by correction-v2.

Item 59 remains complete.

Current authoritative P2 freeze hashes remain:

- design input: `D65E88E8A49114554DBF9C911144BF998172FDD494757AEB6F4491EB9C1F88AF`
- task selection: `829CD09A84616C5282AFAED1FB4EDA168BE238E344E02EC8AE54A8C11D6ADE94`
- curation package: `5832646A66A37E7F47F124796EEE4E289F72584424E37E2E600970FDC929F6A6`

## Correct accepted-boundary/verifier rule

The unit is a validated accepted work-package boundary, not whole-repository greenness.

Do NOT run full `dotnet build SearchForCars.sln` or `dotnet test SearchForCars.sln` as a P2 eligibility, historical-POST or verifier-completion gate.

Verifier qualification must use actual candidate semantics through the narrowest legitimate implementation-independent seam.

Permitted techniques include:

- direct domain/application-service probes;
- minimal private probe projects referencing only the relevant candidate code/dependencies;
- reflection/dynamic loading over stable contracts;
- process/public API behaviour;
- deterministic dependency fakes/adapters;
- stable filesystem/config/output behaviour where the frozen contract calls for it.

Targeted compilation may be used to make a semantic probe executable, but the entire historical solution is not required to be green.

Before treating any inability to use one seam as Terminal B, try reasonable alternate seams.

## P1 precedent

P1 final semantic verifiers used implementation-independent behaviour probes, negative controls, alternate valid implementations, self-tests and deterministic PRE/POST runs. They removed exact test-method selectors/source/diff/commit assertions.

The P2 worker should adapt that proven pattern to the five P2 tasks rather than introducing a new full-build requirement.

## Still-invalid prior evidence

The fixture-only P2 `oracle_runner.py` qualification remains invalid and quarantined because it read hand-authored behavioural-state JSON rather than exercising candidate code.

## Active next work package

**P2 GENUINE SEMANTIC VERIFIER IMPLEMENTATION — P1-PATTERN, NO FULL-SOLUTION BUILD GATE**

Expected success:

- five task verifiers qualified;
- all 18 behaviours semantically observed;
- PRE overall FAIL / POST PASS through genuine semantic seams;
- 18 real semantic negative controls detected;
- alternate-valid implementation independence qualification complete;
- self-tests and determinism PASS;
- hidden-material ACL isolation PASS;
- five final verifier hashes + overall package hash frozen;
- P2 experimental model runs remain 0.

After acceptance, advance to **60/67** and begin item 61 final P2 preregistration/execution lock.