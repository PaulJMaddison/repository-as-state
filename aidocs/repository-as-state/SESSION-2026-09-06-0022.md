# Repository-as-State — Session — 2026-09-06 Item 65G-R2

## Programme state

- 64/67 complete.
- Item 65 ACTIVE.
- 65A–65F complete.
- 65G active.
- 65H–65J pending.

## Result reviewed

The latest zero-model 65G-R1 worker established that the standalone `codex sandbox` wrapper is not the runtime used by accepted Subject-B P2. The wrapper accepts a workspace-only managed profile schema but requires the elevated Windows backend; elevation remains prohibited.

Accepted P2 instead used normal `codex exec` under the restricted non-admin identity with `[windows] sandbox = "unelevated"`.

The worker did not run a Subject-C task model or completion, did not modify the public repository, and did not create a fresh execution lock.

Coordinator classification:

`STANDALONE_CODEX_SANDBOX_WRAPPER_NOT_EQUIVALENT_TO_ACCEPTED_P2_EXECUTION_RUNTIME`

Public record:

- `results/public/subject-c-level3-65g-runtime-path-diagnosis-v1.json`
- `results/public/subject-c-level3-65g-runtime-path-diagnosis-v1.md`

## Methodology repair

The old requirement that the standalone `codex sandbox` wrapper itself perform an exact-runtime zero-model workspace gate is retired before any fresh Subject-C model activity because the wrapper is not equivalent to the accepted execution runtime.

65G-R2 must instead prove, pre-model:

1. runtime/config/launcher equivalence to accepted P2's normal `codex exec` + non-elevated path;
2. zero-model read/create/modify/delete access to representative fresh Subject-C workspaces under the same restricted identity;
3. protected private/source/verifier/coordinator roots remain denied through the filesystem boundary;
4. fresh schedule/blind/PRE/execution state remains unchanged and uncontaminated.

If all four pass, freeze a qualification package. Do not start 65H in the same worker. A later zero-model step finalises the fresh execution lock.

## Scientific constraints unchanged

- failed first Subject-C block permanently closed;
- Slots 1–3 never rerun;
- no project build/compiler/typechecker/public tests/lint for correctness;
- hidden semantic verifier remains the correctness mechanism after complete output freeze;
- no correctness inspection during execution;
- no Subject-C task-model activity before fresh execution-lock acceptance.
