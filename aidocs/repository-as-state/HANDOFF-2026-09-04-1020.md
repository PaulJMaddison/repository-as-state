# Repository-as-State handoff — 2026-09-04 10:20 Europe/London

## Authoritative programme state

- Complete: **57/67**
- Active: **#58**
- #59 blocked behind #58
- #60 blocked behind #59
- P2 experimental runs: **0**
- P2 task-solving model invoked: **FALSE**
- P2 executed: **FALSE**
- P2 preregistered: **FALSE**
- P0 rerun: **FALSE**
- P1 rerun: **FALSE**

## Corrected P2 corpus — accepted

Eligibility re-audit SHA256:

`D55B610B6E2FFB1B032137F30C348FEFE9EF3542321720090C6690885D749E41`

Final eligible candidates:

`C07, C08, C09, C10`

Order:

`C07 → C08 → C09 → C10`

Corpus:

- 4 tasks
- 13 genuine governing behaviours
- 3 LOW / 1 MEDIUM / 0 HIGH

C06 is ineligible. C07 remains eligible with two genuine behaviours; its historical B02 is removed as over-curated non-delta.

## Substantive item-58 design result

The latest worker returned `VALID_TERMINAL_A_ITEM58_DESIGN_REFROZEN` and substantively validated:

- 4 tasks × 3 repetitions × 2 conditions = **24 runs**
- 18 design invariants audited
- 3 independent persistent A chains × 4 chronological tasks = 12 A runs
- 12 fresh independent B sessions
- 12 matched task×repetition units
- fresh exact PRE for every run
- no generated-code carry
- A continuity = reasoning/session continuity only
- full-block blindness over all 24 outputs
- resource telemetry retained separately from correctness
- retry/no-human-rescue principles retained
- descriptive replication only
- no formal equivalence/non-inferiority
- no post-hoc task/repetition/sample manipulation
- build/test/compiler commands = 0
- experimental runs = 0

Reported identities:

- design repair SHA256: `D4CCB92E84CC8112EF304E1A674F5E6D98185CD0B212390514B49563632311A4`
- design freeze SHA256: `782ADD46591FCCDCABB796D25DC3BE71B03627FCC082372B1FD018875DE1248A`
- design package SHA256: `C5124105BD1701A2336211776AF1E3D7E1ED98270665ABAA39910385B81C41F3`

## Narrow integrity blocker

The worker reported:

`P2_LEVEL2_INVARIANT_AUDIT_V2_SHA256=E7D6D969B24F7152DDFFCB32AF06E7C5752BA81B74FF6BD1`

This is only **48 hex characters**, not a valid 64-character SHA-256 digest, while also claiming repeat-match TRUE.

Therefore the coordinator did **not** accept item 58 yet.

The next worker must inspect the actual private v2 design root:

`C:\Kyntic\ras-p2-private\subject-b-p2-v1\level2-design-v2`

and verify:

1. actual SHA256 of `P2-LEVEL2-INVARIANT-AUDIT-v2.json` twice;
2. that the value is exactly 64 hex characters;
3. every reference to that hash inside the design repair, design freeze, contents manifest and package;
4. package contents and deterministic package identity;
5. whether the terminal response alone was malformed or whether an internal artifact is malformed.

If terminal-response-only defect:

- do not regenerate artifacts;
- return the correct full hash and verify all dependent identities unchanged.

If internal artifact defect:

- fix the binding deterministically;
- regenerate every dependent artifact/package;
- return new full SHA256 identities for all affected artifacts;
- preserve prior malformed attempt as non-authoritative evidence if appropriate.

Do not re-run design reasoning unless the private binding audit exposes a substantive contradiction.

## Absolute prohibition

No:

- `dotnet build`
- `dotnet test`
- `dotnet restore`
- compiler/MSBuild/CSC
- candidate execution
- semantic probe compilation
- P2 task-solving model
- P2 experimental run
- item-59 work
- public GitHub modifications by the worker

## Acceptance consequence

If the private identities are clean and a valid full invariant-audit SHA256 is supplied, coordinator may:

- accept item 58;
- advance to **58/67**;
- publish public-safe item-58 design repair/freeze summary;
- activate item 59 corrected four-task contract freeze.

If the private package is internally inconsistent, remain at 57/67 until corrected and rehashed.
