# Subject-B final SearchForCars recovery audit

The only remaining three-task recovery chain was audited from the historical SearchForCars chronology, merge metadata, committed product/architecture/session documentation, source changes and tests.

`RC03_B4_TO_PR8_COMPOSITE` is not coherent: it combines the free/paid product-moat and retained-market-memory programme with the separate PR8 agentic market-acquisition hardening programme.

`RC04_PR8_TO_B5_REMAINDER` is not coherent: it combines owned-market acquisition and retained listing memory with independently scoped security/transport, distributed lease/runtime, commerce/Trader, legal-URL and SDK/release hardening.

Both chain identities are exact, but endpoint identity does not establish work-package coherence. No task contracts were frozen and no discriminator, experimental agent or P0 run occurred. No new SHA was searched.

Next step: `NEW_CORPUS_PROTOCOL_DESIGN`.
## Scope clarification

This recovery failure does **not** erase the earlier SearchForCars corpus-feasibility result.

Before the later comprehensive-verifier amendment, Subject-B had already established a three-task sequential historical chain `WP04_PR5 -> WP05_PR6 -> WP06_PR7`, with exact state identity, implementation-independent narrow behavioural verifiers, PRE-fail/POST-pass discrimination for the three chain tasks, and behavioural negative controls. The aggregate status at that stage was `SUBJECT_B_WORK_PACKAGE_CORPUS_FEASIBLE=true`.

The later comprehensive-verifier standard was a stricter protocol amendment added during curation before any P0 A/B execution. Under that stronger standard, WP04 failed because all eight frozen requirements were already satisfied by PRE, and the subsequent replacement-chain recovery attempts also failed. Therefore the precise current status is:

- SearchForCars corpus feasibility under the original narrow protocol: **proven**;
- SearchForCars final-lock readiness under the later comprehensive protocol: **not established**;
- final recovery path under the comprehensive protocol: **invalid**;
- SearchForCars as a research subject: **not closed or disproven**;
- P0 experimental runs: **zero**.

The next step is a protocol-version decision, not automatic replacement of SearchForCars with another repository.

