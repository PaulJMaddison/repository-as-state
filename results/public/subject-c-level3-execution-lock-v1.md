# Subject C Level-3 execution lock — accepted

The final zero-model execution lock for Subject C is accepted.

The lock binds **18 scheduled runs**, **18 neutral blind IDs**, **18 isolated source-only PRE workspaces**, the accepted Phase-A and Phase-B v2 evidence, the qualified source/state semantic verifier, the fixed task prompts, the restricted runtime and the no-retry/no-rescue execution policy.

All workspaces passed the future-history, private-material and reparse-point escape gates. The condition mapping remains sealed.

The execution block is intentionally source/state only. The task agents must not run project builds, compilers/typecheckers, public tests or lint. These are not correctness signals in this experiment.

The hidden semantic verifier must not be run against any model output until all **18 candidate outputs** have been frozen. Correctness remains fully blinded through the execution block.

The accepted execution-lock package SHA-256 is:

`9538cc1b9af91b732127d08085a7f2211f37f15fd5bd091d8b03a2493f770afe`

Item 65 remains active and Item 66 remains blocked until the execution and subsequent blind adjudication are accepted.
